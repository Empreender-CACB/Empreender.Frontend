name: CI

on:
  push:
    branches:
      - Elstar

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        
      - name: Deploy
        run: echo "Deploying to server..."

        - name: Deploy
        run: |
          echo "Deploying to server..."
          ssh -i ~/.ssh/id_rsa root@167.99.157.110 "mkdir teste"